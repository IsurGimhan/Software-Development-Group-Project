import 'package:flutter/material.dart';

class TreeInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('tree info page'),
      ),
      body: Container(),
    );
  }
}